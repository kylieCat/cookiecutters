{
  "services": {
    "http": {
      "listen": "",
      "port": "{{ cookiecutter.port }}"
    }
  },
  "metrics": {
    "enabled": true,
    "endpoint": "/metrics",
    "port": "{{ cookiecutter.prometheus_port }}"
  }
}
